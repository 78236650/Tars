# 风电智能配载

风电设备（叶片、塔筒、机舱）在重吊船上的智能配载方案生成。

**工具**：`local:wind_stowage_solve`

**触发词**：风电配载、wind stowage、T110、叶片配载、船载方案

**使用方式**：
当用户要求为风电设备生成配载方案时，调用 `local:wind_stowage_solve` 工具。
结果包含：最优套数、各舱口货物分布、旁通板使用量、约束校验结果。
