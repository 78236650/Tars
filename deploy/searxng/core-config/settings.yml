# Minimal override for TARS web_search JSON API
use_default_settings: true

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "tars-local-searxng-change-in-production"

search:
  formats:
    - html
    - json
