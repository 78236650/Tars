# TARS v5.0.0 Phase 1 — 身份与上下文 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 单组织 `org_default` + 可区分的 `user_id`；Principal/审计不再把 `tenant_id` 当成 `user.id`。

**Architecture:** 引入 `tars.org.ORG_ID` 常量；`api/_auth.resolve_authenticated_principal` 对所有用户返回 `tenant_id=ORG_ID`（数据作用域=组织池）；登录审计同步。JWT/contextvars/前端在 Task 2–5。

**Tech Stack:** Python 3.11, FastAPI, pytest, 现有 `UserStore` / PBKDF2

**Design SSOT:** `docs/superpowers/plans/2026-05-30-tars-v5.0.0-multiuser-design.md`

---

### Task 1: ORG_ID 常量与 Principal org 语义

**Files:**
- Create: `backend/tars/org.py`
- Modify: `backend/tars/api/_auth.py`
- Modify: `backend/tars/main.py`（登录审计 `tenant_id`）
- Modify: `backend/tests/test_auth_helper.py`
- Create: `backend/tests/test_org_scope.py`
- Test: `backend/tests/test_auth_helper.py`, `backend/tests/test_org_scope.py`

- [x] **Step 1: Write the failing test**

```python
# backend/tests/test_org_scope.py
from tars.api._auth import resolve_authenticated_principal
from tars.org import ORG_ID


class StubUser:
    def __init__(self, uid, role="user"):
        self.id = uid
        self.role = role
        self.role_template_id = "standard"


class StubStore:
    def __init__(self, mapping):
        self.mapping = mapping

    def get_user_by_api_key(self, k):
        return self.mapping.get(k)


def test_two_users_same_org_scope():
    store = StubStore({
        "k-a": StubUser("user-a"),
        "k-b": StubUser("user-b"),
    })
    pa = resolve_authenticated_principal("k-a", None, None, store)
    pb = resolve_authenticated_principal("k-b", None, None, store)
    assert pa.user_id == "user-a"
    assert pb.user_id == "user-b"
    assert pa.tenant_id == ORG_ID
    assert pb.tenant_id == ORG_ID
    assert pa.tenant_id == pb.tenant_id
```

- [x] **Step 2: Run test to verify it fails**

Run: `cd backend && python -m pytest tests/test_org_scope.py::test_two_users_same_org_scope -v`  
Expected: FAIL (`ORG_ID` or wrong `tenant_id`)

- [x] **Step 3: Implement**

`backend/tars/org.py`:

```python
"""Single-organization scope (v5.0). Multi-org SaaS is out of scope."""
ORG_ID = "org_default"
```

`backend/tars/api/_auth.py`: import `ORG_ID`; set `tenant_id = ORG_ID` for all users; ignore `X-Tenant-Id` (update module docstring).

`backend/tars/main.py`: login audit `tenant_id=ORG_ID` (import from `tars.org`).

- [x] **Step 4: Run tests**

Run: `cd backend && python -m pytest tests/test_auth_helper.py tests/test_org_scope.py -v`  
Expected: PASS (update `test_auth_helper` expectations: `tenant_id == ORG_ID`)

- [x] **Step 5: Commit** (pending — user may commit when ready)

---

**Phase 1 status (2026-05-30):** Tasks 1–5 DONE.

**Phase 2 status (2026-05-30):** T2.1–T2.5 DONE.

**Phase 3 status (2026-05-30):** T3.1–T3.4 DONE (driver shim, PG schema, FTS/ILIKE, migrate_sqlite_to_pg, docker-compose postgres + workers=1). PG integration tests skip without `TARS_TEST_DATABASE_URL`.

```bash
git add backend/tars/org.py backend/tars/api/_auth.py backend/tars/main.py backend/tests/test_org_scope.py backend/tests/test_auth_helper.py
git commit -m "feat(v5): introduce ORG_ID and unify Principal org scope"
```

---

### Task 2: JWT 签发/校验 + auth_tokens 表

**Files:**
- Create: `backend/tars/gateway/jwt_auth.py`
- Modify: `backend/tars/database/connection.py`（`auth_tokens` 表）
- Modify: `backend/tars/database/user_store.py` 或新 `auth_token_store.py`
- Modify: `backend/tars/main.py`（`POST /api/auth/login` 返回 token）
- Create: `backend/tests/test_jwt_auth.py`

（按 design doc T1.2 分步实施；本 plan 文件 Task 2 起详见 v5.0 design §三 Phase 1。）

---

### Task 3–5

- Task 3: `tars/context.py` + `UserContextMiddleware`（T1.3）
- Task 4: JWT 接入 `require_authenticated_user`（T1.4）
- Task 5: 前端 Bearer + WS token（T1.5）

---

## Self-Review（Phase 1）

| Design 要求 | Task |
|-------------|------|
| T1.1 ORG_ID | Task 1 |
| T1.2 JWT | Task 2 |
| T1.3 contextvars | Task 3 |
| T1.4 _auth SSOT | Task 4 |
| T1.5 前端/WS | Task 5 |
