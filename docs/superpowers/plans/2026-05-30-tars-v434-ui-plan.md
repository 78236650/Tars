# TARS v4.3.4 UI/交互优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不改业务逻辑的前提下,把 TARS 前端从"功能可用但视觉凌乱"收敛为"统一、清爽、有设计语言"的界面。

**Architecture:** 走 **A 收敛精修**——保留深棕黑 + amber 气质,不重塑视觉。先把散落的 85 处硬编码棕黑色 + 117 处 slate/stone 混用收口成 `tailwind.config.js` 里的语义化 token;再抽 BaseButton/BaseInput/BaseCard 三个原子组件消灭三套写法;最后做间距/圆角/空态打磨。token 用语义命名为未来亮色模式预留接口,但本轮不实现亮色,不做响应式。

**Tech Stack:** Vue3 + Vite + Tailwind CSS 3.4(扩展 theme)、现有自建组件、vitest(已有 `*.spec.ts`)。

**决策锚点(用户已确认):**
1. 视觉走向 = A 收敛精修(保留深棕黑+amber,只统一+打磨)
2. 响应式 = 不做(内网桌面工具,大屏为主)
3. 亮色模式 = 不做,但 token 用语义变量命名预留接口
4. 交付 = 详细方案 + 执行步骤文件,存 docs

**现状标尺(已核实):**
- `tailwind.config.js` 只 extend slate/blue;stone/amber 走 Tailwind 默认,与硬编码棕黑色不同体系
- `style.css` 无任何 `@apply`/`btn-*`;`btn-primary` 是 8 个组件各自的裸 class
- 硬编码棕黑色 85 处 / 41 文件;`bg-slate`/`bg-stone` 混用 117 处

---

## File Structure(决策锁定)

**第 1 层 token 收口:**
- `frontend/tailwind.config.js` — extend 语义 token:`surface.{0,1,2}` / `border.{DEFAULT,strong}` / `content.{primary,secondary,muted}` / `accent.{DEFAULT,hover,soft}` + 圆角 `card`/阴影 `panel`
- `frontend/src/style.css` — body 与滚动条改用 token(CSS 变量),为亮色预留 `:root` 接口

**第 2 层原子组件:**
- `frontend/src/components/common/BaseButton.vue` — variant: primary/secondary/ghost/danger,带 focus 态
- `frontend/src/components/common/BaseInput.vue` — 统一输入框(含 textarea slot)
- `frontend/src/components/common/BaseCard.vue` — 统一卡片容器

**第 3 层打磨(替换为主,少量新建):**
- 全站替换硬编码色 → token;替换裸 btn → BaseButton
- `frontend/src/components/common/EmptyState.vue` — 统一空态

---
# PHASE 1 — 设计 token 收口(根因,最高性价比)

## Task 1: 在 tailwind.config.js 定义语义 token

**Files:**
- Modify: `frontend/tailwind.config.js`

把散落的棕黑色固化成语义 token。语义命名(surface/content/accent)而非具体色名,为未来亮色预留。

- [ ] **Step 1: 扩展 theme.extend**

把 `tailwind.config.js` 的 `theme.extend` 整段替换为:

```javascript
extend: {
  colors: {
    surface: { 0: '#0c0b09', 1: '#14110f', 2: '#1a1511', 3: '#1c1917' },
    border: { DEFAULT: 'rgba(245,158,11,0.10)', strong: 'rgba(245,158,11,0.20)' },
    content: { primary: '#f5f5f4', secondary: '#d6d3d1', muted: '#a8a29e' },
    accent: { DEFAULT: '#d97706', hover: '#b45309', soft: 'rgba(217,119,6,0.16)' },
    slate: { 900: '#0f172a', 800: '#1e293b', 700: '#334155', 600: '#475569', 500: '#64748b', 400: '#94a3b8', 300: '#cbd5e1' },
    blue: { 600: '#2563eb', 700: '#1d4ed8' },
  },
  borderRadius: { card: '1rem' },
  boxShadow: { panel: '0 30px 100px rgba(8,7,5,0.55)' },
},
```

> 保留 slate/blue 不删,避免现有 117 处引用一次性炸裂;它们在后续 Task 中逐步迁走。

- [ ] **Step 2: 验证构建不破**

Run: `cd frontend && npm run build 2>&1 | tail -5`
Expected: build 成功(新增 token 不影响现有 class)。

- [ ] **Step 3: 验证新 token 可用**

在任意组件临时加 `class="bg-surface-1 text-content-primary"`,
Run: `cd frontend && npx tailwindcss -i src/style.css -o /tmp/t.css --content "./src/**/*.vue" 2>&1 | tail -2 && grep -c "bg-surface-1\|text-content-primary" /tmp/t.css`
Expected: 输出 ≥ 1(token 被编译进 CSS)。然后撤销临时 class。

- [ ] **Step 4: Commit**

```bash
git add frontend/tailwind.config.js
git commit -m "feat(ui): add semantic design tokens (surface/content/accent) to tailwind config"
```

---

## Task 2: style.css 改用 token + 预留亮色接口

**Files:**
- Modify: `frontend/src/style.css`

- [ ] **Step 1: 用 CSS 变量重写全局样式**

把 `style.css` 中 `* {...}` 之后的内容替换为:

```css
:root {
  --surface-0: #0c0b09;
  --content-primary: #f5f5f4;
  --scrollbar-track: #171411;
  --scrollbar-thumb: #4b3a25;
  --scrollbar-thumb-hover: #7c5b2a;
}

body {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  background-color: var(--surface-0);
  color: var(--content-primary);
  min-height: 100vh;
}

::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-track { background: var(--scrollbar-track); }
::-webkit-scrollbar-thumb { background: var(--scrollbar-thumb); border-radius: 3px; }
::-webkit-scrollbar-thumb:hover { background: var(--scrollbar-thumb-hover); }
```

- [ ] **Step 2: 验证全局观感不变**

Run: `cd frontend && npm run dev` — 打开页面,确认背景/滚动条与改前一致(纯等价替换,只是换成变量)。

- [ ] **Step 3: Commit**

```bash
git add frontend/src/style.css
git commit -m "refactor(ui): move global colors to CSS variables, reserve light-mode seam"
```

---
# PHASE 2 — 原子组件统一(回报最大)

> **现状 bug**:`btn-primary` 在 8 个组件被引用,但全站(`style.css`/`index.css`)**无定义**——这些按钮现在缺样式。Task 3 顺手修掉。

## Task 3: BaseButton 组件 + 定义 btn-* 类

**Files:**
- Create: `frontend/src/components/common/BaseButton.vue`
- Create: `frontend/src/components/common/BaseButton.spec.ts`
- Modify: `frontend/src/style.css`(用 `@layer components` 补 `btn-primary`/`btn-secondary` 定义)

- [ ] **Step 1: 写测试**

写入 `frontend/src/components/common/BaseButton.spec.ts`:

```typescript
import { mount } from '@vue/test-utils'
import { describe, it, expect } from 'vitest'
import BaseButton from './BaseButton.vue'

describe('BaseButton', () => {
  it('renders slot and applies variant class', () => {
    const w = mount(BaseButton, { props: { variant: 'primary' }, slots: { default: 'Go' } })
    expect(w.text()).toBe('Go')
    expect(w.classes().join(' ')).toContain('btn-primary')
  })
  it('disables when disabled prop set', () => {
    const w = mount(BaseButton, { props: { disabled: true } })
    expect(w.attributes('disabled')).toBeDefined()
  })
})
```

Run: `cd frontend && npx vitest run src/components/common/BaseButton.spec.ts`
Expected: FAIL(组件不存在)。

- [ ] **Step 2: 实现 BaseButton**

写入 `frontend/src/components/common/BaseButton.vue`:

```vue
<script setup lang="ts">
defineProps<{ variant?: 'primary' | 'secondary' | 'ghost' | 'danger'; disabled?: boolean }>()
</script>

<template>
  <button
    :class="`btn-${variant ?? 'primary'}`"
    :disabled="disabled"
    class="inline-flex items-center justify-center gap-1.5 rounded-card px-4 py-2 text-sm font-medium transition-colors focus:outline-none focus:ring-2 focus:ring-accent/40 disabled:opacity-50 disabled:cursor-not-allowed"
  >
    <slot />
  </button>
</template>
```

- [ ] **Step 3: 在 style.css 补 variant 定义**

在 `frontend/src/style.css` 末尾追加:

```css
@layer components {
  .btn-primary { @apply bg-accent text-content-primary hover:bg-accent-hover; }
  .btn-secondary { @apply bg-surface-2 text-content-secondary border border-border hover:border-border-strong; }
  .btn-ghost { @apply bg-transparent text-content-secondary hover:bg-surface-1; }
  .btn-danger { @apply bg-red-900/30 text-red-300 hover:bg-red-900/50; }
}
```

Run: `cd frontend && npx vitest run src/components/common/BaseButton.spec.ts`
Expected: PASS(2 passed)。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/common/BaseButton.vue frontend/src/components/common/BaseButton.spec.ts frontend/src/style.css
git commit -m "feat(ui): add BaseButton and define btn-* variants (fixes undefined btn-primary)"
```

---

## Task 4: BaseInput + BaseCard 组件

**Files:**
- Create: `frontend/src/components/common/BaseInput.vue`
- Create: `frontend/src/components/common/BaseCard.vue`
- Create: `frontend/src/components/common/BaseInput.spec.ts`

照现有真实输入框样式(`AddToolModal.vue:61`)抽取,换成 token。

- [ ] **Step 1: 写 BaseInput 测试**

写入 `frontend/src/components/common/BaseInput.spec.ts`:

```typescript
import { mount } from '@vue/test-utils'
import { describe, it, expect } from 'vitest'
import BaseInput from './BaseInput.vue'

describe('BaseInput', () => {
  it('emits update:modelValue on input', async () => {
    const w = mount(BaseInput, { props: { modelValue: '' } })
    await w.find('input').setValue('hi')
    expect(w.emitted('update:modelValue')?.[0]).toEqual(['hi'])
  })
})
```

Run: `cd frontend && npx vitest run src/components/common/BaseInput.spec.ts`
Expected: FAIL(组件不存在)。

- [ ] **Step 2: 实现 BaseInput**

写入 `frontend/src/components/common/BaseInput.vue`:

```vue
<script setup lang="ts">
defineProps<{ modelValue?: string; placeholder?: string }>()
defineEmits<{ 'update:modelValue': [v: string] }>()
</script>

<template>
  <input
    :value="modelValue"
    :placeholder="placeholder"
    class="w-full rounded-card border border-border bg-surface-1 px-3 py-2 text-content-primary placeholder:text-content-muted focus:outline-none focus:ring-2 focus:ring-accent/40"
    @input="$emit('update:modelValue', ($event.target as HTMLInputElement).value)"
  />
</template>
```

- [ ] **Step 3: 实现 BaseCard**

写入 `frontend/src/components/common/BaseCard.vue`:

```vue
<template>
  <div class="rounded-card border border-border bg-surface-1 p-4">
    <slot />
  </div>
</template>
```

Run: `cd frontend && npx vitest run src/components/common/BaseInput.spec.ts`
Expected: PASS。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/common/BaseInput.vue frontend/src/components/common/BaseCard.vue frontend/src/components/common/BaseInput.spec.ts
git commit -m "feat(ui): add BaseInput and BaseCard atomic components"
```

---

# PHASE 3 — 迁移替换 + 打磨(体力活,回报立竿见影)

> 标尺:硬编码棕黑色 85 处 / 41 文件;slate/stone 混用 117 处。按域分批替换,每批一个 commit,每批后 `npm run build` + 肉眼验证。

## Task 5: 替换硬编码色为 token(按域分批)

**Files:**
- Modify: 41 个含硬编码棕黑色的 `.vue`(分 4 批:layout / chat / memory / 其余)

- [ ] **Step 1: 批 1 — layout 域**

把 `src/components/layout/*.vue` 中:`bg-[#0c0b09]`→`bg-surface-0`、`bg-[#14110f]`→`bg-surface-1`、`bg-[#1a1511]`→`bg-surface-2`、`bg-[#1c1917]`→`bg-surface-3`、`rgba(217,119,6,0.16)`→`accent-soft`。
Run: `cd frontend && grep -rln "#0c0b09\|#14110f\|#1a1511\|#1c1917" src/components/layout/`
Expected: 替换后无输出。然后 `npm run build` 通过 + dev 肉眼比对。Commit: `refactor(ui): migrate layout components to design tokens`。

- [ ] **Step 2: 批 2 — chat 域**

同样映射,作用于 `src/components/chat/*.vue` + `src/views/ChatView.vue`。
Run: `cd frontend && grep -rln "#0c0b09\|#110f0d\|#171411\|#1a1511" src/components/chat/`
Expected: 无输出。`npm run build` + 肉眼验证主聊天界面。Commit: `refactor(ui): migrate chat components to design tokens`。

- [ ] **Step 3: 批 3 — memory 域**

作用于 `src/components/memory/*.vue` + `src/views/MemoryView.vue`。
Run: `cd frontend && grep -rln "#171411\|#1a1511" src/components/memory/`
Expected: 无输出。`npm run build` + 验证。Commit: `refactor(ui): migrate memory components to design tokens`。

- [ ] **Step 4: 批 4 — 其余(insight/bi/knowledge/tools/settings)**

Run: `cd frontend && grep -rln "#0c0b09\|#110f0d\|#171411\|#1a1511\|#14110f\|#1c1917" src/ --include="*.vue"`
Expected: 全部替换后**无输出**(85 处清零)。`npm run build` + 全局点查。Commit: `refactor(ui): migrate remaining components to design tokens`。

---

## Task 6: 裸 btn 替换为 BaseButton + 空态统一

**Files:**
- Create: `frontend/src/components/common/EmptyState.vue`
- Modify: 8 个用 `btn-primary` 的组件 + 含手写空态的组件

- [ ] **Step 1: 实现 EmptyState**

写入 `frontend/src/components/common/EmptyState.vue`:

```vue
<script setup lang="ts">
defineProps<{ text?: string }>()
</script>

<template>
  <div class="flex flex-col items-center justify-center gap-2 py-12 text-content-muted">
    <slot name="icon" />
    <p class="text-sm">{{ text ?? '暂无数据' }}</p>
  </div>
</template>
```

- [ ] **Step 2: 替换裸 `<button class="btn-primary">` 为 `<BaseButton variant="primary">`**

在 8 个组件(KnowledgeManager / WorkflowStrip / DataSourceSettings / DocumentDetailDrawer / SchemaAnnotator / InsightView / BiAnalyticsView)中,import BaseButton 并替换。逐文件 `npm run build` 验证。

- [ ] **Step 3: 验证 + Commit**

Run: `cd frontend && npm run build 2>&1 | tail -3 && npx vitest run 2>&1 | tail -5`
Expected: build 成功;已有测试(含 KnowledgeManager.spec.ts)零新增失败。

```bash
git add frontend/src/components/common/EmptyState.vue frontend/src/components/
git commit -m "refactor(ui): replace raw buttons with BaseButton, unify empty states"
```

---

## 验收(全计划完成后)

- [ ] `grep -rln "#0c0b09\|#110f0d\|#171411\|#1a1511\|#14110f\|#1c1917" frontend/src --include="*.vue"` → **无输出**(85 处硬编码色清零)
- [ ] `btn-primary` 有定义且全站按钮样式一致(现状 bug 已修)
- [ ] `cd frontend && npm run build` 成功 + `npx vitest run` 零新增失败
- [ ] dev 跑起来,主聊天/Memory/Knowledge 三个核心界面肉眼确认:配色统一、按钮/输入框/卡片一致
- [ ] token 用语义命名,未来加亮色只需补 `:root` 变量(本轮不实现)

## 显式排除(YAGNI,本轮不做)

- ❌ 不重塑视觉(保留深棕黑+amber,见决策 A)
- ❌ 不做响应式断点(内网桌面工具)
- ❌ 不实现亮色模式(仅语义 token 预留接口)
- ❌ 不清理 slate/blue token(现有 117 处引用,本轮不强制迁移,留待后续)
- ❌ 不补全 aria/键盘导航(可访问性单列未来版本,避免本轮膨胀)

