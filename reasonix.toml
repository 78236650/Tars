# Reasonix configuration.
# Resolution order: flag > ./reasonix.toml > ~/.config/reasonix/config.toml > built-in defaults.
# Secrets come from the environment via api_key_env; never put keys here.

config_version = 2   # schema marker for diagnostics; old versions may ignore it
default_model = "deepseek-flash"
# language      = "zh"   # ui/model language; empty = auto-detect from $LANG / $REASONIX_LANG

[agent]
# system_prompt = """..."""   # omit to use the built-in prompt for this version
# system_prompt_file = "prompts/system.md"   # overrides system_prompt when set
max_steps         = 0   # executor tool-call rounds; 0 = no limit
planner_max_steps = 12   # planner read-only tool-call rounds; 0 = no limit
temperature       = 0.0
auto_plan   = "off"   # off|on; off keeps plan mode manual
# auto_plan_classifier = "deepseek-flash"   # optional; only used for borderline tasks
soft_compact_ratio  = 0.5   # notice only; keeps cache-first prefix intact
compact_ratio       = 0.8   # try compacting when prompt reaches this fraction
compact_force_ratio = 0.9   # force compacting at this high-water mark
# planner_model = "mimo"   # optional: enable two-model collaboration
# subagent_model = "deepseek-pro"   # optional default for runAs=subagent skills
# subagent_models = { review = "deepseek-pro", security_review = "deepseek-pro" }   # per-skill overrides
# subagent_effort = "high"   # optional default effort for subagents
# subagent_efforts = { review = "max", task = "high" }   # per-tool/skill effort overrides
# output_style = "explanatory"   # explanatory | learning | concise | custom; empty = default

[tools]
enabled = []   # empty = all built-in tools
bash_timeout_seconds = 120   # foreground safety cap; set 0 for no tool-local cap

[codegraph]
enabled      = true   # built-in MCP server; off by default for first-run sessions
auto_install = true   # fetch the runtime when CodeGraph is enabled but missing
# path       = ""   # empty = cache, then PATH, then a bundle beside reasonix

[lsp]
enabled = true   # language server tools; servers launch lazily when used
# [lsp.servers.go]
# command = "gopls"
# args = []
# extensions = [".go"]

[skills]
# paths = ["~/my-skills", "../shared/skills"]   # extra custom skill roots
# excluded_paths = ["~/.agents/skills"]   # hide convention roots without deleting folders
# max_depth = 3   # nested scan depth; set 1 for legacy root-only discovery
# disabled_skills = ["review"]   # hide noisy or unwanted skills

[permissions]
# Per-call gating. mode = writer fallback when no rule matches: ask|allow|deny.
# Readers always default to allow. Precedence: deny > ask > allow > fallback.
# Rules are "Tool" or "Tool(specifier)"; e.g. Bash(go test:*), Edit(src/**).
mode  = "ask"
# deny = ["Bash(rm -rf*)", "Bash(git push*)"]   # hard-blocked in every mode
allow = ["bash=curl -sI --connect-timeout 10 https://github.com 2>&1 | head -5", "bash=curl -v --connect-timeout 10 https://github.com 2>&1 | tail -20", "bash=echo \"HTTP_PROXY=$HTTP_PROXY\"; echo \"HTTPS_PROXY=$HTTPS_PROXY\"; echo \"http_proxy=$http_proxy\"; echo \"https_proxy=$https_proxy\"; echo \"NO_PROXY=$NO_PROXY\"", "write_file=.trae/documents/2026-06-08-multimodal-generation-plan.md", "bash=lsof -ti:8000 -ti:5173 2>/dev/null", "bash=grep -n '__main__\\|uvicorn\\.run\\|if __name__' backend/tars/main.py", "bash=cd /Users/daobanxiang/myproject/TARS/backend && source .venv/bin/activate && python -m tars.main", "bash=cd /Users/daobanxiang/myproject/TARS/frontend && npm run dev", "write_file=.trae/documents/2026-06-09-memory-evolution-design.md", "write_file=.trae/documents/2026-06-09-memory-evolution-execution-plan.md", "bash=docker info 2>/dev/null | head -5 || echo \"Docker not available in this environment\"", "bash=cd /Users/daobanxiang/myproject/TARS && docker compose -f deploy/docker-compose.yml ps 2>/dev/null", "bash=lsof -i :8000 2>/dev/null | head -10", "bash=lsof -i :5173 2>/dev/null | head -10", "bash=lsof -i :8080 2>/dev/null | head -10", "bash=ls -la /Users/daobanxiang/myproject/TARS/scripts/restart-tars-dev.sh 2>/dev/null", "bash=pgrep -f \"uvicorn\" 2>/dev/null; pgrep -f \"tars.main\" 2>/dev/null; pgrep -f \"vite\" 2>/dev/null", "bash=cd /Users/daobanxiang/myproject/TARS && bash scripts/restart-tars-dev.sh 2>&1", "bash=lsof -i :8000 -i :5173 2>/dev/null | grep -E \"LISTEN|COMMAND\"", "Bash(pkill -f \"python -m tars.main\" 2>/dev/null; pkill -f \"uvicorn\" 2>/dev/null; echo \"Killed old processes\")", "Bash(cat /Users/daobanxiang/myproject/TARS/.env 2>/dev/null | grep -i jwt || echo \"No JWT secret found in .env\")", "Bash(cat /Users/daobanxiang/myproject/TARS/.env 2>/dev/null)", "Edit(/Users/daobanxiang/myproject/TARS/.env)", "Bash(ls /Users/daobanxiang/myproject/TARS/backend/.venv/bin/python 2>/dev/null || ls /Users/daobanxiang/myproject/TARS/backend/venv/bin/python 2>/dev/null || echo \"No venv found\")", "Bash(cd /Users/daobanxiang/myproject/TARS/backend && .venv/bin/python -m tars.main > nohup-backend.log 2>&1 & echo \"Backend PID: $!\")", "Bash(sleep 8 && tail -30 /Users/daobanxiang/myproject/TARS/backend/nohup-backend.log 2>/dev/null)", "Bash(curl -s http://localhost:8000/docs 2>&1 | head -5; echo \"---\"; curl -s http://localhost:8000/api/auth/login -X POST -H \"Content-Type: application/json\" -d '{\"username\":\"admin\",\"password\":\"admin123\"}' 2>&1 | head -20)", "Bash(lsof -i :8000 2>/dev/null; echo \"exit: $?\")", "Bash(lsof -i :5173 2>/dev/null; echo \"frontend exit: $?\")", "Bash(curl -s -X POST http://localhost:8000/api/auth/login -H \"Content-Type: application/json\" -d '{\"username\":\"admin\",\"password\":\"admin123\"}' | python3 -m json.tool 2>&1)", "Bash(curl -s -X POST http://localhost:8000/api/auth/login -H \"Content-Type: application/json\" -d '{\"identifier\":\"admin\",\"password\":\"admin123\"}' | python3 -m json.tool 2>&1)", "Bash(cd /Users/daobanxiang/myproject/TARS/backend && source .venv/bin/activate && python3 -c \"\nimport sqlite3\nconn = sqlite3.connect('tars.db')\ncur = conn.cursor()\ncur.execute('SELECT id, username, role, created_at FROM users')\nrows = cur.fetchall()\nfor r in rows:\n    print(r)\nconn.close()\n\")", "Bash(cd /Users/daobanxiang/myproject/TARS/backend && source .venv/bin/activate && python3 -c \"\nimport sqlite3\nconn = sqlite3.connect('tars.db')\ncur = conn.cursor()\n# list all tables\ncur.execute(\\\"SELECT name FROM sqlite_master WHERE type='table' ORDER BY name\\\")\ntables = [r[0] for r in cur.fetchall()]\nprint('Tables:', tables)\n# Try users\ncur.execute('SELECT COUNT(*) FROM users')\nprint('users count:', cur.fetchone()[0])\n# Check if there's a different auth table\nif 'admins' in tables:\n    cur.execute('SELECT COUNT(*) FROM admins')\n    print('admins count:', cur.fetchone()[0])\nif 'auth_tokens' in tables:\n    cur.execute('SELECT COUNT(*) FROM auth_tokens')\n    print('auth_tokens count:', cur.fetchone()[0])\nconn.close()\n\")", "Bash(curl -s -X POST http://localhost:8000/api/auth/setup -H \"Content-Type: application/json\" -d '{}' 2>&1 | python3 -m json.tool || echo \"setup not available\")", "Bash(cd /Users/daobanxiang/myproject/TARS/backend && source .venv/bin/activate && python3 -c \"\nimport sys\nsys.path.insert(0, '.')\nfrom tars.database import Database\nfrom tars.database.user_store import UserStore\nfrom tars.models.user import UserRole\n\ndb = Database()\nuser_store = UserStore(db)\n\n# Check existing\nusers = user_store.get_all_users()\nprint(f'Existing users: {len(users)}')\nfor u in users:\n    print(f'  {u.username} ({u.role.value})')\n\n# Create admin\ntry:\n    user = user_store.create_user('admin', 'admin@tars.local', UserRole.ADMIN, password='admin123')\n    print(f'Created: {user.username} ({user.role.value}) id={user.id}')\nexcept Exception as e:\n    print(f'Error: {e}')\n\")", "Bash(cd /Users/daobanxiang/myproject/TARS/backend && source .venv/bin/activate && python3 -c \"\nimport sys\nsys.path.insert(0, '.')\nfrom tars.database import Database\nfrom tars.database.user_store import UserStore\nfrom tars.gateway.permission import UserRole\n\ndb = Database()\nuser_store = UserStore(db)\n\nusers = user_store.get_all_users()\nprint(f'Existing: {len(users)}')\ntry:\n    user = user_store.create_user('admin', 'admin@tars.local', UserRole.ADMIN, password='admin123')\n    print(f'Created admin id={user.id}')\nexcept Exception as e:\n    import traceback\n    traceback.print_exc()\n\")", "Bash(cd /Users/daobanxiang/myproject/TARS/backend && source .venv/bin/activate && python3 -c \"\nimport sys, os\nsys.path.insert(0, '.')\nos.environ['DATABASE_PATH'] = './data/tars.db'\nfrom tars.database import Database\nfrom tars.database.user_store import UserStore\n\ndb = Database()\nuser_store = UserStore(db)\n\nusers = user_store.get_all_users()\nprint(f'Total users: {len(users)}')\n# Find admin user\nfor u in users:\n    if hasattr(u.role, 'value') and u.role.value == 'admin':\n        print(f'Admin found: {u.username} {u.email} id={u.id}')\n# Also check by username\nfor u in users:\n    if u.username == 'admin':\n        print(f'User admin: {u.username} {u.email} id={u.id} role={u.role}')\n\" 2>/dev/null)", "Bash(cd /Users/daobanxiang/myproject/TARS/backend && source .venv/bin/activate && python3 -c \"\nimport sys, os, hashlib, secrets\nsys.path.insert(0, '.')\nos.environ['DATABASE_PATH'] = './data/tars.db'\n\n# Generate password hash the same way UserStore does\npassword = 'admin123'\nsalt = secrets.token_hex(16)\nstretched = hashlib.pbkdf2_hmac('sha256', password.encode(), salt.encode(), 100000).hex()\npassword_hash = f'pbkdf2:sha256:100000:{salt}:{stretched}'\nprint(f'New hash: {password_hash[:60]}...')\n\nfrom tars.database import Database\ndb = Database()\nconn = db._get_conn()\nconn.execute('UPDATE users SET password_hash = ? WHERE username = ?', (password_hash, 'admin'))\nconn.commit()\nprint('Password reset for admin')\nconn.close()\n\" 2>/dev/null)", "Bash(cd /Users/daobanxiang/myproject/TARS/backend && source .venv/bin/activate && python3 -c \"\nimport sys, os, hashlib, secrets\nsys.path.insert(0, '.')\nfrom tars.database.user_store import UserStore\n\npassword = 'admin123'\nus = UserStore.__new__(UserStore)\nsalt = secrets.token_hex(16)\ndigest = hashlib.pbkdf2_hmac('sha256', password.encode('utf-8'), salt.encode('utf-8'), 100000)\npassword_hash = f'pbkdf2-sha256\\$100000\\${salt}\\${digest.hex()}'\n\n# Verify it\nok = us._verify_password_hash(password, password_hash)\nprint(f'Self-verify: {ok}')\n\nfrom tars.database import Database\nos.environ['DATABASE_PATH'] = './data/tars.db'\ndb = Database()\nconn = db._get_conn()\nconn.execute('UPDATE users SET password_hash = ? WHERE username = ?', (password_hash, 'admin'))\nconn.commit()\nprint('Admin password reset to admin123')\nconn.close()\n\" 2>/dev/null)", "Bash(cd /Users/daobanxiang/myproject/TARS/backend && source .venv/bin/activate && python3 << 'PYEOF'\nimport sys, os, hashlib, secrets\nsys.path.insert(0, '.')\nos.environ['DATABASE_PATH'] = './data/tars.db'\nfrom tars.database.user_store import UserStore\n\nus = UserStore.__new__(UserStore)\npassword = 'admin123'\nsalt = secrets.token_hex(16)\ndigest = hashlib.pbkdf2_hmac('sha256', password.encode('utf-8'), salt.encode('utf-8'), 100000)\npassword_hash = f'pbkdf2-sha256$100000${salt}${digest.hex()}'\n\n# Print parts for debug\nprint(f'algo: pbkdf2-sha256')\nprint(f'salt: {salt}')\nprint(f'digest first 20: {digest.hex()[:20]}')\nprint(f'full hash: {password_hash}')\nok = us._verify_password_hash(password, password_hash)\nprint(f'self-verify: {ok}')\n\nfrom tars.database import Database\ndb = Database()\nconn = db._get_conn()\nconn.execute('UPDATE users SET password_hash = ? WHERE username = ?', (password_hash, 'admin'))\nconn.commit()\nconn.close()\nprint('Done')\nPYEOF\n 2>/dev/null)", "Bash(cd /Users/daobanxiang/myproject/TARS/backend && source .venv/bin/activate && python3 << 'PYEOF'\nimport sys, os, hashlib, secrets\nsys.path.insert(0, '.')\nos.environ['DATABASE_PATH'] = './data/tars.db'\n\npassword = 'admin123'\nsalt = secrets.token_hex(16)\ndigest = hashlib.pbkdf2_hmac('sha256', password.encode('utf-8'), salt.encode('utf-8'), 100000)\npassword_hash = f'pbkdf2_sha256$100000${salt}${digest.hex()}'\n\n# verify manually\nalgo, iters, s, exp = password_hash.split('$', 3)\nd2 = hashlib.pbkdf2_hmac('sha256', password.encode('utf-8'), s.encode('utf-8'), int(iters))\nimport hmac\nok = hmac.compare_digest(d2.hex(), exp)\nprint(f'verify: {ok}')\n\nfrom tars.database import Database\ndb = Database()\nconn = db._get_conn()\nconn.execute('UPDATE users SET password_hash = ? WHERE username = ?', (password_hash, 'admin'))\nconn.commit()\nconn.close()\nprint('Password reset OK')\nPYEOF\n 2>/dev/null)", "Bash(# Test file upload with the token\nTOKEN=\"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiI0YTg2MzYyNS02YmMxLTQ3MmQtOGNlMS1jODM1MTFjOTVlNDkiLCJvcmdfaWQiOiJvcmdfZGVmYXVsdCIsImp0aSI6IjcxMDEwMzQ4LTAzMDktNDFlYi04MDI3LWIzZjQ5Mjg1MzgyZCIsImV4cCI6MTc4MTE5MTgwOH0.0kqaqNp3zf0Be2yzcK9Eagow2r0wN8eX6iZSIZ6YYMw\"\n\n# Test upload endpoint\necho \"test content\" > /tmp/test_upload.txt\ncurl -s -X POST http://localhost:8000/api/files/upload \\\n  -H \"Authorization: Bearer $TOKEN\" \\\n  -F \"file=@/tmp/test_upload.txt\" 2>&1 | python3 -m json.tool)", "Bash(curl -s http://localhost:8000/api/files/upload -X OPTIONS -H \"Origin: http://localhost:5173\" -H \"Access-Control-Request-Method: POST\" 2>&1 | head -20\necho \"---\"\n# Also check the files routes\ncurl -s -X GET http://localhost:8000/openapi.json 2>&1 | python3 -c \"import json,sys; d=json.load(sys.stdin); [print(p) for p in sorted(d['paths'].keys()) if 'file' in p.lower()]\")", "Bash(python3 -c \"\nfrom html.parser import HTMLParser\nimport sys\n\nclass TextExtractor(HTMLParser):\n    def __init__(self):\n        super().__init__()\n        self.skip = False\n        self.text = []\n    def handle_starttag(self, tag, attrs):\n        if tag in ('script','style','noscript'):\n            self.skip = True\n    def handle_endtag(self, tag):\n        if tag in ('script','style','noscript'):\n            self.skip = False\n    def handle_data(self, data):\n        if not self.skip:\n            t = data.strip()\n            if t:\n                self.text.append(t)\n\nwith open('/Users/daobanxiang/Downloads/多租户分布式 AI Agent 设计：从功能架构到执行链路的完整拆解.html') as f:\n    content = f.read()\n\nextractor = TextExtractor()\nextractor.feed(content)\n\nprint('\\n'.join(extractor.text))\n\" 2>&1 | head -300)", "Bash(python3 -c \"\nfrom html.parser import HTMLParser\n\nclass TextExtractor(HTMLParser):\n    def __init__(self):\n        super().__init__()\n        self.skip = False\n        self.text = []\n    def handle_starttag(self, tag, attrs):\n        if tag in ('script','style','noscript'):\n            self.skip = True\n    def handle_endtag(self, tag):\n        if tag in ('script','style','noscript'):\n            self.skip = False\n    def handle_data(self, data):\n        if not self.skip:\n            t = data.strip()\n            if t:\n                self.text.append(t)\n\nwith open('/Users/daobanxiang/Downloads/多租户分布式 AI Agent 设计：从功能架构到执行链路的完整拆解.html') as f:\n    content = f.read()\n\nextractor = TextExtractor()\nextractor.feed(content)\nlines = extractor.text\n\n# Find article body start (skip to 开篇导读)\nstart = 0\nfor i, l in enumerate(lines):\n    if '开篇导读' in l:\n        start = i\n        break\nprint('\\n'.join(lines[start:]))\n\" 2>&1)"]
# ask = ["Edit(src/**)"]   # force a prompt even if otherwise allowed

[sandbox]
# Confine tool blast radius. File-writers (write_file/edit_file/multi_edit)
# may only write under workspace_root (empty = current dir) + allow_write.
# bash = "enforce" (default) jails each command in an OS sandbox (macOS now;
# graceful fallback elsewhere); "off" disables it. network allows egress.
# workspace_root = ""            # default: current working directory
# allow_write = ["/tmp"]          # extra dirs writers may also modify
bash    = "enforce"
network = true

[statusline]
# A custom status line: a command whose first stdout line replaces the built-in
# data row. It receives {"model","contextUsed","contextWindow","cwd"} as JSON on stdin.
# command = "my-statusline.sh"

# External MCP servers. type: "stdio" (default, a subprocess) | "http" | "sse".
# ${VAR} / ${VAR:-default} are expanded from the environment in command/args/env/url/headers.
# [[plugins]]
# name    = "example"
# command = "reasonix-plugin-example"
# [[plugins]]                                  # a remote server over Streamable HTTP
# name    = "stripe"
# type    = "http"
# url     = "https://mcp.stripe.com"
# headers = { Authorization = "Bearer ${STRIPE_KEY}" }
